import externalConfig from '@/config/external.config';
import dataProcessingConfig from '@/config/data-processing.config';
import appConfig from '@/config/app.config';


const configs = [externalConfig, dataProcessingConfig, appConfig];

export { configs}